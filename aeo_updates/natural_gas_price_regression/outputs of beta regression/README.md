# Outputs of Beta Regression

This directory stores intermediate files generated by the beta regression step of the pipeline. These files are consumed by downstream steps (sync and alpha regression) and are not final outputs.
