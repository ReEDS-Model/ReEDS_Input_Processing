# Overview
This repo includes several scripts:
- Format H2 costs (storage, pipeline, compressor) for ReEDS inputs (`process-h2-production.py`)
- Assign H2 storage types to each county or ba (`calculate_regional_h2_storage_type.py`)
- Calculate H2 demand share by county (`calculate_regional_h2_demand_share.py`)
